/*

package com.example.spotthedifference

import org.junit.Test

class ImageDisplayerTest {

    @Test
    fun testDisplayImage() {
        val imageDisplayer = mock(ImageDisplayer::class.java)
        val image = "imageData" // Remplacez par des données d'image réelles

        imageDisplayer.display(image)

        verify(imageDisplayer).display(image) // Vérifiez que la méthode display a été appelée
    }
}

*/